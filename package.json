{
  "name": "dragtowhatever",
  "version": "1.0.0",
  "private": true,
  "description": "Transform the current page with a processor and drag the artifact anywhere.",
  "author": "imeavesdropping <imeavesdropping.me@gmail.com>",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite build && vite build --mode content --watch",
    "build": "tsc --noEmit && vite build && vite build --mode content",
    "typecheck": "tsc --noEmit",
    "icons": "python3 scripts/generate-icons.py"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/turndown": "^5.0.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vite-plugin-static-copy": "^2.2.0"
  }
}
